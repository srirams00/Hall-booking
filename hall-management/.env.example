# Copy this file to .env and fill in the real values.
# NEVER commit .env to version control.

# Backend API URL (Vite exposes VITE_* prefixed vars to the browser)
VITE_API_URL=http://localhost:5000/api
